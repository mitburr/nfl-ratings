name: "vectors_cumulative"
model_type: "vectors"
parameters:
  elo_config:
    k_factor: 20
    home_advantage: 40
    initial_rating: 1500
  vector_config:
    boost: 0.60
    weight: 0.35
    blend_prior: false
metrics:
  - accuracy
  - log_loss
  - brier_score